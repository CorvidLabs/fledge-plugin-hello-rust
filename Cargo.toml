[package]
name = "fledge-hello-rust"
version = "0.1.0"
edition = "2021"
description = "Example fledge plugin in Rust demonstrating the fledge-v1 protocol"
license = "MIT"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
